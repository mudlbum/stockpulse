Automated draft. **Do not merge without reading it.**

Before merging:

- [ ] Every figure checks out against `data-store/` and the published JSON
- [ ] Nothing reads as a recommendation, a prediction, or a guarantee
- [ ] Set `reviewedBy:` to your actual name — not "PENDING REVIEW"
- [ ] Set `draft: false`

The post will not render while `draft: true`, so merging without the last two
steps is safe but pointless.

Assembled deterministically from computed pipeline figures — no language model
wrote this text, which is why `aiAssisted: false`. If that ever stops being
true, the flag has to change.
